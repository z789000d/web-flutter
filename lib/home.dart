import 'package:flutter/material.dart';

import 'largehomePage.dart';

class Home extends StatelessWidget {
  const Home({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return LargeHomePage();
  }
}
